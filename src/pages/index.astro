---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const allTalks = await getCollection('talks');
const now = new Date();
const rawBase = import.meta.env.BASE_URL;
const base = rawBase.endsWith('/') ? rawBase : `${rawBase}/`;

const upcomingTalks = allTalks
  .filter((talk) => talk.data.date >= now)
  .sort((a, b) => a.data.date.getTime() - b.data.date.getTime());

const pastTalks = allTalks
  .filter((talk) => talk.data.date < now)
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime());

const arrow = '\u2197';

function getCategory(talk: any): string {
  return talk.data.category || 'talk';
}

function formatDate(date: Date): string {
  return date.toLocaleDateString('en-US', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
    timeZone: 'UTC',
  });
}
---

<BaseLayout title="Talks" activePage="talks">
  <div class="section">
    <h2 class="section-title">Upcoming Talks</h2>
    <p class="section-subtitle">Find details about the next talks and what's coming up</p>
  </div>

  <div class="section">
    {upcomingTalks.length === 0 ? (
      <p class="empty-state">
        No upcoming talks at the moment. DM or reply in
        <a href="https://discord.gg/47S3xZ4F">Discord</a> if you want to present!
      </p>
    ) : (
      upcomingTalks.map((talk) => (
        <div class="talk-card">
          <div class="talk-card-content">
            <div class="talk-card-header">
              <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 6.042A8.967 8.967 0 0 0 6 3.75c-1.052 0-2.062.18-3 .512v14.25A8.987 8.987 0 0 1 6 18c2.305 0 4.408.867 6 2.292m0-14.25a8.966 8.966 0 0 1 6-2.292c1.052 0 2.062.18 3 .512v14.25A8.987 8.987 0 0 0 18 18a8.967 8.967 0 0 0-6 2.292m0-14.25v14.25" /></svg>
              <div class="talk-card-title"><a href={`${base}talks/${talk.slug}/`}>{talk.data.title}</a></div>
            </div>
            <div class="talk-card-detail">
              <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M6.75 3v2.25M17.25 3v2.25M3 18.75V7.5a2.25 2.25 0 0 1 2.25-2.25h13.5A2.25 2.25 0 0 1 21 7.5v11.25m-18 0A2.25 2.25 0 0 0 5.25 21h13.5A2.25 2.25 0 0 0 21 18.75m-18 0v-7.5A2.25 2.25 0 0 1 5.25 9h13.5A2.25 2.25 0 0 1 21 11.25v7.5" /></svg>
              <span>{formatDate(talk.data.date)}</span>
              <span class={`talk-type-tag ${getCategory(talk)}`}>{getCategory(talk)}</span>
              {(talk.data.slides || talk.data.recording) && (
                <div class="talk-card-links">
                  {talk.data.slides && <a href={talk.data.slides}>Slides</a>}
                  {talk.data.recording && <a href={talk.data.recording}>Recording</a>}
                </div>
              )}
            </div>
            <div class="talk-card-detail">
              <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M15.75 6a3.75 3.75 0 1 1-7.5 0 3.75 3.75 0 0 1 7.5 0ZM4.501 20.118a7.5 7.5 0 0 1 14.998 0A17.933 17.933 0 0 1 12 21.75c-2.676 0-5.216-.584-7.499-1.632Z" /></svg>
              <span>{talk.data.speaker}</span>
              {talk.data.affiliation && (
                <>
                  <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 21a9.004 9.004 0 0 0 8.716-6.747M12 21a9.004 9.004 0 0 1-8.716-6.747M12 21c2.485 0 4.5-4.03 4.5-9S14.485 3 12 3m0 18c-2.485 0-4.5-4.03-4.5-9S9.515 3 12 3m0 0a8.997 8.997 0 0 1 7.843 4.582M12 3a8.997 8.997 0 0 0-7.843 4.582m15.686 0A11.953 11.953 0 0 1 12 10.5c-2.998 0-5.74-1.1-7.843-2.918m15.686 0A8.959 8.959 0 0 1 21 12c0 .778-.099 1.533-.284 2.253m0 0A17.919 17.919 0 0 1 12 16.5c-3.162 0-6.133-.815-8.716-2.247m0 0A9.015 9.015 0 0 1 3 12c0-1.605.42-3.113 1.157-4.418" /></svg>
                  <span>{talk.data.affiliation}</span>
                </>
              )}
            </div>
            {talk.data.description && <p class="talk-card-description">{talk.data.description}</p>}
          </div>
          {talk.data.image && <img src={talk.data.image} alt="" class="talk-card-image" />}
        </div>
      ))
    )}
  </div>

  <div class="section">
    <h2 class="section-title">Past Talks</h2>
    <p class="section-subtitle">Look back at previous talks and speakers</p>
  </div>

  <div class="section">
    {pastTalks.map((talk) => (
      <div class="talk-card">
        <div class="talk-card-content">
          <div class="talk-card-header">
            <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 6.042A8.967 8.967 0 0 0 6 3.75c-1.052 0-2.062.18-3 .512v14.25A8.987 8.987 0 0 1 6 18c2.305 0 4.408.867 6 2.292m0-14.25a8.966 8.966 0 0 1 6-2.292c1.052 0 2.062.18 3 .512v14.25A8.987 8.987 0 0 0 18 18a8.967 8.967 0 0 0-6 2.292m0-14.25v14.25" /></svg>
            <div class="talk-card-title"><a href={`${base}talks/${talk.slug}/`}>{talk.data.title}</a></div>
          </div>
          <div class="talk-card-detail">
            <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M6.75 3v2.25M17.25 3v2.25M3 18.75V7.5a2.25 2.25 0 0 1 2.25-2.25h13.5A2.25 2.25 0 0 1 21 7.5v11.25m-18 0A2.25 2.25 0 0 0 5.25 21h13.5A2.25 2.25 0 0 0 21 18.75m-18 0v-7.5A2.25 2.25 0 0 1 5.25 9h13.5A2.25 2.25 0 0 1 21 11.25v7.5" /></svg>
            <span>{formatDate(talk.data.date)}</span>
            <span class={`talk-type-tag ${getCategory(talk)}`}>{getCategory(talk)}</span>
            {(talk.data.slides || talk.data.recording) && (
              <div class="talk-card-links">
                {talk.data.slides && <a href={talk.data.slides}>Slides</a>}
                {talk.data.recording && <a href={talk.data.recording}>Recording</a>}
              </div>
            )}
          </div>
          <div class="talk-card-detail">
            <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M15.75 6a3.75 3.75 0 1 1-7.5 0 3.75 3.75 0 0 1 7.5 0ZM4.501 20.118a7.5 7.5 0 0 1 14.998 0A17.933 17.933 0 0 1 12 21.75c-2.676 0-5.216-.584-7.499-1.632Z" /></svg>
            <span>{talk.data.speaker}</span>
            {talk.data.affiliation && (
              <>
                <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 21a9.004 9.004 0 0 0 8.716-6.747M12 21a9.004 9.004 0 0 1-8.716-6.747M12 21c2.485 0 4.5-4.03 4.5-9S14.485 3 12 3m0 18c-2.485 0-4.5-4.03-4.5-9S9.515 3 12 3m0 0a8.997 8.997 0 0 1 7.843 4.582M12 3a8.997 8.997 0 0 0-7.843 4.582m15.686 0A11.953 11.953 0 0 1 12 10.5c-2.998 0-5.74-1.1-7.843-2.918m15.686 0A8.959 8.959 0 0 1 21 12c0 .778-.099 1.533-.284 2.253m0 0A17.919 17.919 0 0 1 12 16.5c-3.162 0-6.133-.815-8.716-2.247m0 0A9.015 9.015 0 0 1 3 12c0-1.605.42-3.113 1.157-4.418" /></svg>
                <span>{talk.data.affiliation}</span>
              </>
            )}
          </div>
          {talk.data.description && <p class="talk-card-description">{talk.data.description}</p>}
        </div>
        {talk.data.image && <img src={talk.data.image} alt="" class="talk-card-image" />}
      </div>
    ))}
  </div>
</BaseLayout>
